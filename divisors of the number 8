/* Print all divisors of the number 8 using critical */

#include <iostream>
#include <omp.h>

int main() 
{

#pragma omp parallel for
	for (int i = 1; i <= 8; i++)
	{
		if (8 % i == 0)
		{
#pragma omp critical
			{
				printf("%d \n", i);
			}
		}
	}
	return 0;
}
