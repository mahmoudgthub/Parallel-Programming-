/*

[ omp_set_num_threads(10) ] 
sets the number of threads to be used in the next parallel region to 10

*/

#include <iostream>
#include <omp.h>
using namespace std;

int main()
{
	//int x;
	//cin >> x;

	omp_set_num_threads(10); //10

#pragma omp parallel // num_threads(5) // if( x == 5 )
	{
		printf("Num of threads: \n");
	}	
}

